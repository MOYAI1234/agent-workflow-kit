# Tool Router — 决策树

> ⚠️ 本文件由 tool-router 初始化流程自动生成。
>
> 如果你看到这段说明，说明还没有执行过初始化。
> 请对你的 agent 说："扫描工作区，初始化工具链" 或 "rebuild tool inventory"。
>
> agent 会自动扫描你的 skills 目录、MCP 连接器、CLI 工具和项目目录，
> 然后用扫描结果覆盖本文件。

---

## 决策树

<!-- 由初始化流程自动生成，格式示例：

### 1. 分类名称

\```
要做什么？
├── 场景A → 工具1（skill）
├── 场景B → 工具2（CLI）
└── 不确定？→ 先试工具X
\```

### 2. 另一个分类

\```
...
\```

-->

## Agent 归属速查

<!-- 由初始化流程自动生成，格式：

| 工具类型 | Agent A | Agent B |
|---------|---------|---------|
| Skills | 全量 | 部分 |
| MCP | 3 个 | 5 个 |

-->

## 维护指南

### 增加新工具

1. 安装 skill/plugin/MCP
2. 编辑 `docs/tools-inventory.md` 添加条目
3. 编辑本文件在对应决策树节点添加分支

### 删除工具

1. 确认无其他工具依赖
2. 从 `tools-inventory.md` 移除
3. 从本文件移除
4. 清理 skill 目录（参考 `/agent-skill-cleanup`）

### 检查冲突

同类工具超过 2 个时，必须在 `tools-inventory.md` 的对比表中写清楚差异和选择规则。

### 增量更新

安装新工具后，可以对 agent 说："扫描新增的工具，更新决策树"。
agent 只扫描新增部分，合并到现有清单中，不会重建整个文件。
